[pytest]
# Avoid traversing system or data directories that are not part of the tests
norecursedirs = DB node_modules .venv venv .git .cache
python_files = test_*.py
filterwarnings = ignore::DeprecationWarning
