services:
  front:
    build:
      target: development
    volumes:
      - ./front:/app
    environment:
      - NUXT_PUBLIC_API_BASE=http://localhost:9001
    env_file:
      - .env.dev
    command: npm run dev
  # api:
  #   volumes:
  #     - ./api:/app
